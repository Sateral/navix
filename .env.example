# App runtime (Supabase transaction pooler :6543 is fine).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/navix?schema=public"
# Prisma CLI (migrate, studio): must not use transaction pooler (:6543) — use session pooler (:5432 on *.pooler.supabase.com) or direct db.<ref>.supabase.co:5432.
DIRECT_URL=""
BETTER_AUTH_SECRET="replace-with-at-least-32-random-characters"
BETTER_AUTH_URL="http://localhost:3000"
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
AI_PROVIDER=""
AI_API_KEY=""
